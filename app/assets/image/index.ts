import Error from "./error-404.png";
import Logo from "./logo.png";
import LogoWithName from "./logoWithName.png";
import middleware from "./middleware.png";

const images = {
  Logo,
  Error,
  LogoWithName,
  middleware,
};

export default images;
